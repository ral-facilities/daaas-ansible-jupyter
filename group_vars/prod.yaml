host_domain: "jupyter.stfc.ac.uk"
ip_address: "130.246.214.172"
staging_cert: false

admin_names:
number_of_users:
username:

profiles:
  - profile1:
    display_name: "Default environment" 
    description: "This is a simple default environment"
    default: true
    image: jupyter/minimal-notebook 
    gpu_required: false
    commands:

iris_iam:
  required: false
  client_id:
  client_secret:
  admin_groups:
  allowed_groups: